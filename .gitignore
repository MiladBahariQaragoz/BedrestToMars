# Source PDFs — copyrighted publisher material, kept out of the repository.
# The reference list and the extraction table are the shareable artefacts.
resources/

# Raw dataset spreadsheets — inclusion decision still open (see README.md)
*.xlsx
*.xls

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/

# Node
node_modules/
dist/
build/
.next/

# Environment
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
Thumbs.db
*.stackdump

# Bulk database exports and the tables merged from them.
# Scopus and Web of Science licence terms restrict redistributing exported records, and
# this repository is public. The files live in the shared Drive folder instead; the counts
# derived from them are committed. See docs/literature-review/PLAN.md section 10.
docs/literature-review/exports/
data/search/all_records.csv
data/search/screening.csv
data/search/.access_cache.json
resources/figures/
